"""Deterministic Java inventory and scanner integration.

The package initializer intentionally has no third-party imports because the
same standard-library analysis modules run in the restricted scanner image.
"""
