# CP0 synthetic fixture authorization record

The Java, XML and JSP files in this directory were authored specifically for
Cairn's public test suite. They contain no commercial vendor code, identifiers,
binaries, decompiled text, credentials or customer data.

The Cairn project may compile, package, statically analyze, decompile and
execute these files for testing. Generated class, JAR, WAR and EAR files must
remain in a temporary or ignored build directory and must not be committed.

This authorization applies only to this project-authored fixture. It grants no
right to acquire, inspect or redistribute any third-party product artifact.
